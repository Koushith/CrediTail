import { Text, View } from "react-native";

export const SplashScreen = () => {
  return (
    <View
      style={{
        flex: 1,
        backgroundColor: "#000",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <Text>CrediTail.....</Text>
    </View>
  );
};
